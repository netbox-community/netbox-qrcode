<!--
    IMPORTANT: Please base this pull request on the `feature` branch, not `main`.

    Active development happens on `feature`, which targets the upcoming NetBox
    minor release. GitHub defaults the base branch to `main`, so please check
    the base branch selector above and change it to `feature` if needed.

    Use `main` only for fixes to an already-released version of the plugin.
-->
### Target branch: feature

<!--
    Please note that pull requests are accepted ONLY for approved
    issues. Specify the issue resolved by this PR below.
-->
### Fixes: #1234

<!--
    Please include a summary of the changes within this PR below.
-->
